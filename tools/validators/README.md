# CRM OS Validators

Validator target command:

```bash
pnpm validate:specs
```

Validation checks required spec files, endpoint permissions, tenant scoped entities, event payload tenantId, and acceptance criteria.

Source status: FROM_CHAT_RECONSTRUCTED.
