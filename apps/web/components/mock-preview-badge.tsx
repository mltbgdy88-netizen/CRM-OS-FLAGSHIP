/** Shared "Canlı önizleme" badge for MOCK screens with demo data. */
export function MockPreviewBadge() {
  return (
    <span className="dashboard-page__live-badge">
      <span className="dashboard-page__live-dot" aria-hidden />
      Canlı önizleme
    </span>
  );
}
