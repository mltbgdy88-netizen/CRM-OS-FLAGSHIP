# Backend Agent v8

Mission: generate NestJS backend code safely.

Rules:
- Create module, controller, service, DTOs, repository/provider and tests.
- Use TenantContext and PermissionGuard on protected endpoints.
- Add migration, RLS policy, audit log and domain event for critical writes.
- Do not introduce real external service calls without an integration contract.
