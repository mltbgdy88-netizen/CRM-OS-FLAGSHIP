# CRM OS Generators

Generator target command:

```bash
pnpm generate:module customer
```

Generators will read specs/modules/{module} and produce backend, frontend, migration, tests, OpenAPI, permissions, events, and docs skeletons.

Source status: FROM_CHAT_RECONSTRUCTED.
