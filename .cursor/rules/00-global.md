# 00 Global Cursor Rules v8

- Read `AGENTS.md`, `MASTER-SOURCE-OF-TRUTH-v8.md`, `.cursor/rules/*` and the active sprint spec before coding.
- Generate small changes that map to one sprint task.
- Do not modify unrelated modules.
- Do not create real API calls during UI-only tasks.
- Do not bypass tenant isolation, permissions, audit logs or quality gates.
- Every feature must include tests, documentation updates and validation notes.
